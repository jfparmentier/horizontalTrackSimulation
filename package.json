{
  "name": "simulation-banc-horizontal-animated-svg",
  "version": "0.9.3",
  "private": true,
  "type": "module",
  "description": "Simulation SVG animée avec banc fixé à 2 m et échelle graphique commune pour S1 et S2.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-standalone.mjs",
    "smoke": "node scripts/smoke-standalone.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  }
}
