{
  "name": "simulation-banc-horizontal-animated-svg",
  "version": "0.9.1",
  "private": true,
  "type": "module",
  "description": "Simulation SVG animée avec état central, mesures de capteurs et export CSV autonome.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-standalone.mjs",
    "smoke": "node scripts/smoke-standalone.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  }
}
