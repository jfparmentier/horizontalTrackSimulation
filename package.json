{
  "name": "simulation-banc-horizontal-animated-svg",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "Simulation SVG enregistrant les mesures exactes de vitesse lors du franchissement des capteurs.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-standalone.mjs",
    "smoke": "node scripts/smoke-standalone.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  }
}
