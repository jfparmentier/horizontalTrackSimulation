{
  "name": "simulation-banc-horizontal-animated-svg",
  "version": "0.9.5",
  "private": true,
  "type": "module",
  "description": "Simulation SVG animée avec S1 fixée à 1 kg, chute fixée à 0,5 m, banc de 2 m et neuf capteurs.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-standalone.mjs",
    "smoke": "node scripts/smoke-standalone.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  }
}
